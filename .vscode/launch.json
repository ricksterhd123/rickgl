{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch HTTP Server",
            "runtimeExecutable": "npx",
            "args": [
                "http-server",
                "build",
                "-p", "3000"
            ],
            "console": "integratedTerminal"
        }
    ]
}
